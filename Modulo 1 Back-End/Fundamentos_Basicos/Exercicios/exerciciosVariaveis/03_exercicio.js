let lado = 5 , largura = 16 , comprimento = 6 , base = 12 , altura = 8 , resposta = 0;

// calculo quadrado 
resposta = lado * lado;
console.log(`A área do Quadrado é ${resposta}`);

// calculo retângulo largura * Comprimento
resposta = largura * comprimento;
console.log(`A área do Retângulo é ${resposta}`);

// calculo triângulo = base *altura / 2
resposta = base * altura / 2;
console.log(`área do triâgulo é ${resposta}`);

let raiz = Math.sqrt(8); // sqrt Raiz quadrada
console.log(raiz);

let potencia = Math.pow(3 ,2); // pow Potencia
console.log(potencia);
